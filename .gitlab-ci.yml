# ---

# default:
#   cache:
#     # TODO:
#     key: tst
#     paths:
#       - /usr/lib/python3.10/

stages:
  - init
  - lint
  # - format
  # - test

init:
  stage: init
  image: snakepacker/python:all
  script:
    - python3.10 -m pip install venv poetry
    - poetry export -f requirements.txt --output requirements.txt --without-hashes
    - python3.10 -m venv .venv
    - source .venv/bin/activate
    - python -m pip install --no-cache-dir --upgrade -r requirements.txt
  artifacts:
      paths:
        - .venv

lint:
  stage: lint
  image: snakepacker/python:3.10
  dependencies:
    - init
  before_script:
    - apt-get update
    - apt-get install build-essential -y
  script:
    - make lint

# # format:
# #   stage: format
# #   image: snakepacker/python:3.10
# #   # dependencies:
# #     # - init
# #   script:
# #     - make format

# # test:
# #   stage: test
# #   image: snakepacker/python:3.10
# #   # dependencies:
# #     # - init
# #   script:
# #     - make test
